import { StyleSheet } from "react-native";

export const TextStyles = StyleSheet.create({
    baseText: {
        fontFamily: "Cochin"
    },
    titleText: {
        color: "white",
        fontSize: 20,
        fontWeight: "bold"
    }
});