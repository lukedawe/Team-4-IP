import { StyleSheet } from "react-native";

const PickerStyle = StyleSheet.create({
    container:{
        flex: 1,
        paddingTop: 40,
        alignItems: "center"
    }
});

export default PickerStyle;